generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SiteContent {
  id           String   @id @default(cuid())
  key          String   @unique
  heroTitle    String
  heroSubtitle String
  updatedAt    DateTime @updatedAt
}
